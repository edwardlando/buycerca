<%= form_for(@group) do |f| %>
  <% if @group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group.errors.count, "error") %> prohibited this group from being saved:</h2>

      <ul>
      <% @group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <li class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name, :placeholder => "Group name" %> 
  </li>

  <li class="field">
    <%= f.label :avatar, "Group image" %><br />
    <%= f.file_field :avatar %>
  </li>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
