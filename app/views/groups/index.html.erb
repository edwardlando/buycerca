
<% if user_signed_in? %>
	Hello, <%= link_to current_user.name, current_user %>
<% else %>
	<%= link_to "Sign in with Facebook", user_omniauth_authorize_path(:facebook) %>
<% end %>

<br/>

<%= link_to "SELL", new_item_path, :class => "btn" %> <br/>

<h1>Find your community</h1>

<div class="row">
<% @groups.each do |g| %>
	  <div class="group_tile"><%= g.name %><%= link_to image_tag(g.avatar.url(:medium)), g %></div> 
<% end %>
</div>